import 'package:flutter/material.dart';

class Followings extends StatelessWidget {
  const Followings({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Followings');
  }
}